{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="page-btn" type="submit">Сохранить</button>
    <a class="page-btn" href="{{ path('app_settings_index') }}">Отмена</a>
{{ form_end(form) }}
